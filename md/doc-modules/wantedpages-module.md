# WantedPages Module

> Documentation > Modules > WantedPages Module

This module lists all pages that do not exist but there are links that point to them.

The same module can be found in **site tools** at the bottom of every page.

### Usage

```
[[module WantedPages]]
```
