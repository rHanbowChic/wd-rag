# wd-rag
另一份wikidot文档，主要用于检索增强生成（RAG）。

文档在 `md` 目录下，均为标准的 Markdown 格式。

`md` 目录也是一个 VitePress 项目的根目录，这个网站是 RAG 的在线知识库。

## 运行顺序
`extract_links.py` -> `scrape_docs.py` -> `conver_to_md.py` -> `split_faq.py`
